{{#each brands}}
  <div>
    {{this.name}}
    <a href="/brands/{{this.id}}/edit">Изменить</a>
    <form action="/brands/{{this.id}}/delete" method="POST">
      <input type="hidden" name="_method" value="DELETE">
      <button type="submit">Удалить</button>
    </form>
  </div>
{{/each}}