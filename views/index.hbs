<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="keywords" content="гитара, музыкальный инструмент">
  <meta name="description" content="Интернет магазин гитар">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/css/{{cssFile}}">
  {{> scripts}}
</head>
<body class="body">
{{> header}}
<main>
  {{#if isAuthenticated}}
    <div class="auth-panel">
      <p>Добро пожаловать, {{username}}!</p>
      <a href="/?logout=true" class="logout-button">Выйти</a>
    </div>
  {{else}}
    <div class="auth-form">
      <h3>Авторизация</h3>
      <form id="loginForm">
        <input type="text" name="username" placeholder="Логин" required>
        <input type="password" name="password" placeholder="Пароль" required>
        <button type="submit">Войти</button>
      </form>
    </div>
  {{/if}}
  {{> (lookup . 'body')}}
</main>
{{> footer}}
{{#if additionalScripts}}
  {{#each additionalScripts}}
    <script src="{{this}}" defer></script>
  {{/each}}
{{/if}}
<div class="server-time">
  Server processing time: {{elapsedTime}}ms
</div>
</body>
</html>